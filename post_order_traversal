
void print_inorder(Node * root)
{
    if (root == NULL)
    {
        return ;
    }
    print_pos_order(root->left);
    
    print_post_order(root->right);
    
    cout<<root->data<<" "<<endl;
    
}
